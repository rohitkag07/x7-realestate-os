PORT=8083
NODE_ENV=production
AGENT_SECRET=replace-with-shared-secret

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# External executors (via tool-gateway, same pattern as Phase 2)
TOOL_GATEWAY_URL=http://localhost:8081

# OpenAI for calendar generation
OPENAI_API_KEY=
OPENAI_MODEL_CONTENT=gpt-4o

# Remotion render + Higgsfield are reached through the tool-gateway
